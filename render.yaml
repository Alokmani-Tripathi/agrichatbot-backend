services:
  - type: web
    name: agrichatbot-backend
    env: python
    region: singapore
    plan: free
    pythonVersion: 3.11.6
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port 
